import 'package:flutter/material.dart';

class FreeCookie extends StatelessWidget {
  const FreeCookie({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
